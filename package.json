{
  "name": "heavy-computation-ecs",
  "version": "0.1.0",
  "bin": {
    "heavy-computation-ecs": "bin/heavy-computation-ecs.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "bootstrap": "cdk bootstrap",
    "deploy": "cdk deploy",
    "test": "jest"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.79.0",
    "aws-cdk": "1.79.0",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "@aws-cdk/aws-ec2": "^1.79.0",
    "@aws-cdk/aws-ecs": "^1.79.0",
    "@aws-cdk/aws-ecs-patterns": "^1.79.0",
    "@aws-cdk/core": "1.79.0"
  }
}
